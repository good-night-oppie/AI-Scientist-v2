# Kaggle ladder probe — submission `54585744`

_Submitted: 2026-07-11T21:32:07Z_

## Authorization

Parent `rpo` independently reviewed the candidate and posted A2A shared-log
`#2852`: **APPROVED for one Kaggle ladder probe**, subject to four conditions:

1. Rebuild through the committed `build_submission_search.py` validation path.
2. Label the submission as deck-only with deck hash and search-fallback caveat.
3. Keep s14 as champion; if the probe scores below s14, do not submit another deck
   without new reconfirmed evidence.
4. One slot only; no chained same-day submissions without fresh rpo review.

No harness/Eddie veto appeared before submission.

## Rebuilt artifact

- Candidate: `2c1368bc03`
- Deck SHA-256:
  `d65f30c88b5c5ba0ea24e073bc38103ac190f2abee5b0839cce8d3eab54ef3e2`
- Rebuilt bundle:
  `/home/admin/gh/ready-player-one-ptcg/submission_search_deck_2c1368bc03.tar.gz`
- Bundle SHA-256:
  `de9865df385b419ca7e3f613b4d136bfcf5cc172af516c7915a2b1e3d7b14347`
- Bundle size: `502407` bytes
- Exact candidate deck bytes are present as `deck.csv`.
- Legal composition: 60 cards, 2 Kyogre, 34 basic Water Energy, maximum 4
  non-basic-energy copies per card name.
- `main.py`, `agpkg/policy.py`, `agpkg/search_policy.py`, and
  `agpkg/cg/libcg.so` hashes equal the frozen live-s14 baseline.

## Runner-faithful validation

Fresh rebuild validation completed 20 games:

```text
self-play vs random: 18W 2L 0D inv0 | winrate 90.0% over 20 | 0.22s/game
SEARCH BEGIN CALLS: 20 across 20 games
SEARCH INACTIVE/FALLBACK: calls <= games; for live-s14, calls == games is the known first-move failure signature
VALIDATION OK — runner-faithful load, zero forfeits, search_active=False
```

## Submission

Command surface: `uvx --from kaggle kaggle competitions submit`.

Description:

```text
rpo-approved single deck-only probe 2c1368bc03; deck sha256 d65f30c88b5c5ba0ea24e073bc38103ac190f2abee5b0839cce8d3eab54ef3e2; frozen s14 policy; known search fallback search_active=false; s14 remains champion
```

- Kaggle reference: `54585744`
- Initial status: `SubmissionStatus.PENDING`
- Terminal validation: `SubmissionStatus.COMPLETE`
- Initial public score: `600.0`
- Private score: not available
- Same-poll comparison: probe `600.0`, s14 `580.7`, seed `521.0`.
- s14 `54554870` remains the other scored submission; the probe displaced only the
  seed slot as parent review predicted.

## Interpretation and next gate

The probe validated and its initial rating is `19.3` points above the same-poll s14
snapshot. This is ladder support for the deck change, not settled superiority: the
lane's observed rating volatility is much larger than this gap and no private score
exists. Do not submit another deck without fresh rpo review. The bounded deck
search and final harvester continue.
