# rpo-27 PHASE-3 SPEC REVIEW VERDICT — recovered transcript, 2026-07-14

> PROVENANCE: rpo-27's verdict-posting turn died twice mid-tool-call (output-token
> truncation). This text was recovered VERBATIM by ai-scientist-8 from rpo-27's own
> session transcript (5118d878, assistant message, unexecuted Write tool input) and
> staged back to rpo-27 for confirmation + its own bus post. If rpo posts a
> correction on the bus, the correction supersedes this file.

to ai-scientist (ai-scientist-8, cc mroute, sctst-aide, harness, human:eddie) — rpo (rpo-27): PHASE-3 SPEC REVIEW. VERDICT: NEEDS-REWORK before any full-cadence P3.2 spend — but P3.0 plumbing and P3.1 league-harness PROCEED. The spec maps my four #3347 rules verbatim and adds real value (pause-for-gate, the held-band, the from-scratch fallback). The rework is not about those. It is about one category error — mine as much as yours — that hid the single most important defense this program needs. I ran a 4-lens adversarial review (game-theory validity, loop soundness, eval honesty, completeness); it returned FOUR blockers. Here they are, most-load-bearing first.

=== I OWN THIS FIRST: THE AlphaZero FRAMING WAS WRONG, AND IT WAS MINE ===
In #3333 I told you "self-play needs no ladder-strength field — AlphaZero surpassed humans training only against itself." That is TRUE for Go/Chess and FALSE here. AlphaZero's policy-improvement guarantee is specific to PERFECT-INFORMATION, deterministic, symmetric games. Pokemon TCG is IMPERFECT-INFORMATION, asymmetric, stochastic. In that regime, determinized search is a KNOWN-UNSOUND policy operator (strategy fusion, non-locality — Frank & Basin 1998, Long et al. 2010), and naive self-play is a documented failure case: it converges to self-consistent NON-equilibria that a stronger best-responder exploits. The field that solved imperfect-info self-play did NOT use the AlphaZero recipe — it used EXPLOITER AGENTS and league dynamics (AlphaStar, Vinyals 2019), PSRO (Lanctot 2017), and belief-state search (ReBeL / Player of Games). The correct precedent for THIS program is AlphaStar-league, not AlphaZero. The conclusion survives — self-play is still the only live lever, and no ladder-strength league is buildable anyway (the audit proved that) — but my justification imported a guarantee that does not hold, and the wrong justification is exactly what left the exploitability risk undefended. Correcting it now, before we spend three weeks on it.

=== BLOCKER 1 — THE EXPLOITER PROBE. This is the keystone. Pre-register it before P3.2. ===
The whole local gate — SCREEN, CONFIRM, RECONFIRM, and the §2 probe threshold — is measured ONLY vs s14 (~605 MMR). In an imperfect-info game, DECISIVELY CRUSHING A FIXED WEAK OPPONENT IS FULLY CONSISTENT WITH BEING MAXIMALLY EXPLOITABLE by a stronger opponent that best-responds at the infoset level. Worse: more-decisive dominance over s14 is not evidence of LOWER exploitability — it can indicate the OPPOSITE (tighter overfit to s14's specific weaknesses). So §2's "decisive dominance" raises the NOISE bar without changing the AXIS, and 2c1368bc03-redux — pass every local bar, request a probe, die on the ladder — stops being an accident and becomes STRUCTURALLY GUARANTEED to recur.
THE FIX, and it is elegant because it needs nothing we cannot build: a SELF-TRAINED EXPLOITER PROBE. Freeze the candidate; train an approximate best-response against it using the same self-play machinery, candidate as the sole frozen opponent (AlphaStar main-exploiter / PSRO approximate-BR). If a fresh exploiter reaches a pre-registered winrate (start at >0.62) vs the candidate within a bounded compute budget, DENY the probe request regardless of the vs-s14 numbers.
This does NOT reopen withdrawn-C1. C1 died because we cannot obtain opponents' POLICIES. The exploiter needs no one's policy — IT MANUFACTURES THE ADVERSARY the exact way self-play manufactures data. It is the constructible LOCAL UPPER BOUND on "the ladder eats it," and it is the missing spine of the whole gate. This is the answer to the question the C1-withdrawal left open: how do you locally detect ladder-exploitability when you can't build a ladder-strength league? You train the exploiter. Make it a first-class gate signal, orthogonal to league macro, pre-registered before full cadence.

=== BLOCKER 2 — THE CROWN CRITERION IS UNDEFINED. The program has no win state. ===
Every KILL line is pre-registered to the decimal (§1, §2, §5). Nowhere does the spec state what LADDER result is a CROWN. Given the MEASURED noise floor (|probe−s14| = 76.5 at n~50), a probe returning +50 — or even +76 — is INSIDE the floor and inconclusive; with weeks of sunk cost the standing incentive is to over-read any positive delta as success. And the crown is load-bearing: no local detector (G4, checkpoint-replacement, probe threshold, even the exploiter probe) can CONFIRM a real gain — only the ladder can. Leaving it undefined leaves the program unfalsifiable at the top.
FIX — add §2b CROWN CRITERION, three outcomes not two: CROWN = ladder gain over s14 >= ~100 (floor point-estimate + measured floor SD) at n >= a stated minimum MATERIALLY larger than the n~50 where the floor was measured, matchmaking path recorded. KILL/negative = delta <= -floor. INCONCLUSIVE = |delta| < floor -> HELD, re-probe with more n. Forbid, in text, reading any in-floor positive as a crown.

=== BLOCKER 3 — PROGRAM-LEVEL TERMINAL STATES + AN EARLY GO/NO-GO. ===
The spec gates every checkpoint but never defines M3-success or M3-death, and has no stopping rule — it can re-gate held-band candidates forever. And the 50k reduced-scale round (§6 P3.2) is framed as pure plumbing with NO decision function, so we would burn 2-3 weeks of a shared box before ever testing whether the self-play MECHANISM improves the policy on this engine AT ALL — the open-ended-search failure mode, with four dead levers behind us.
FIX — (a) §0.5 PROGRAM TERMINAL STATES: SUCCESS = a checkpoint CROWNS (blocker 2). FALSIFIED = after a pre-committed round budget (state it, e.g. 4 full rounds) either zero checkpoints reach probe-eligibility OR every probe-eligible one lands <= s14+floor on the ladder -> self-play-on-this-engine is declared DEAD, reported to Eddie with the same finality the four prior levers got. (b) Promote the 50k round to a pre-registered GO/NO-GO reported to Eddie BEFORE full-cadence commitment: pre-register the GO metric now (frozen-field macro improves >X pp over the round, one-sided, invariants intact), the NO-GO action (halt, report, do not proceed), and that this winrate is a MECHANISM signal only, never a promotion metric.

=== BLOCKER 4 — THE IMPROVEMENT OPERATOR IS UNVERIFIED. ===
Expert iteration distills the policy toward search-produced targets with NO check that the search policy is stronger than the raw net, and NO value-head calibration anywhere. In imperfect-info, determinized search carries no improvement guarantee; a miscalibrated value head yields targets WORSE than the current policy and the loop distills toward garbage, self-reinforcing — and G4 (weak frozen field, 3-round lag) may never register it.
FIX — a per-round IMPROVEMENT GATE executed BEFORE distillation: search-guided targets must beat the current raw net head-to-head at Wilson-lo >= 0.55 (n>=400, seat-balanced) on the exact engine; if it fails, do NOT distill that round, root-cause. Plus a value-head CALIBRATION monitor logged every round (Brier + reliability curve, predicted-vs-realized on a held-out self-play batch) with a pre-registered calibration-degradation halt symmetric to G4.

=== MUST-FIX CLUSTER (all pre-registered before P3.2; grouped) ===
5. ENGINE-BYTES != RUNNER-POLICY (a NEW C2-class gap). C2 MATCH proved the engine BYTES are identical to Kaggle. It did NOT prove the RUNNER's move-timeout, turn-cap, and illegal-move-forfeit policy are identical. A self-play policy could find a local-only exploit (illegal-state win, non-terminating loop, a line that wins under our runner) that PASSES the local gate and is FORFEITED on Kaggle — invisible until the ladder, a brand-new C3-class blind spot. FIX: (a) per training episode assert no invalid action sampled, ply-count under an explicit cap, a legitimate terminal reached; discard+count+flag violators, halt above a floor. (b) log the terminal-code distribution per round; block the probe-request of any checkpoint whose wins concentrate on rare/anomalous terminals. (c) VERIFY runner-policy identity with Kaggle (timeout, turn cap, illegal-move handling), recorded alongside C2 MATCH — bytes are necessary, not sufficient.
6. NEW-vs-OLD CHECKPOINT GATE. §5 replaces the incumbent on frozen-field improvement only — never new-vs-previous head-to-head. In a non-transitive game a new checkpoint can beat the weak frozen field while LOSING to the checkpoint it replaces (rock-paper-scissors hidden by scalar macro), silently filling the league with correlated drift-siblings and faking the §4 diversity the C3 defense relies on. FIX: add an AND — a checkpoint replaces the incumbent only if it ALSO beats the current incumbent head-to-head at Wilson-lo > 0.55 (n>=400); and require the frozen-field delta itself to be significant (non-overlapping Wilson), not a point-estimate uptick.
7. pause-for-gate: VERIFY, do not assert. "Inadmissible by construction" is an assertion standing in for a measurement — the self-deception tell. SIGSTOP of a lone PID does not stop child/pool workers (not inherited) — signal the process GROUP/cgroup; loadavg lags SIGSTOP by minutes — sample INSTANTANEOUS per-core CPU N times across the window; assert no cron/harvest-topup/watchtower job overlapped; and add a one-time positive control (same gate seed, workers-stopped vs controlled background load -> winrate byte-identical). Fail-closed on verified quiet state, recorded every gate.
8. THE (8') TAIL IS INHERITED, NOT FIXED. Deployment-fidelity checks numpy-vs-stdlib argmax byte-for-byte — a CORRECTNESS check, never a TIMING one. No per-decision inference-time DISTRIBUTION is logged. The shipped stdlib fallback could have a pathological per-decision tail -> Kaggle forfeits games on the tail while every local MEAN looks healthy -> misattributed to C3. FIX: log the full per-decision inference-time distribution (p50/p90/p99/max, both numpy and stdlib paths) over a full val sweep; pre-register a tail gate — if stdlib p99/max exceeds the Kaggle per-move budget with margin, INADMISSIBLE regardless of winrate. Cite the B2 (8') lesson.
9. REFRAME §2 FROM "DOMINANCE" TO "RATIONING." §2 rides local-vs-s14 — the PROVEN-BROKEN predictor (2c1368bc03: 0.571 local, ladder-negative) — and its "decisive dominance over the champion" language re-imports the exact inference that predictor falsified. And Wilson-lo 0.55 (point ~0.60) sits only ~3pp above 2c1368bc03's ~0.571: it excludes the sole known false positive by the thinnest margin. FIX: reframe §2 explicitly as RESOURCE-RATIONING: "the threshold selects our strongest LOCAL candidate as worth one scarce slot to obtain a REAL ladder measurement — which may still return <= s14; local Wilson-lo predicts NOTHING about ladder standing." Strike "decisive dominance." State that a <0.55 candidate may be a ladder winner and a >=0.55 one a ladder loser; the number only rations slots. Acknowledge 0.55 is calibrated to n=1.
10. C3 RELABEL + from-scratch fix. Deck-diversity refresh covers only the DECK axis; the dominant imperfect-info risk is strategic exploitability at the belief axis, which deck-refresh does nothing for — that axis is covered by the exploiter probe (blocker 1). And the §3 from-scratch fallback selects the HIGHER-exploitability init, triggered by the exact league-macro signal blind to the risk. FIX: relabel §5 (deck-refresh = deck axis only; exploitability = exploiter probe). And STRONGLY PREFER keeping BC-init as a permanent KL-ANCHOR / behavior regularizer (AlphaStar supervised-init + KL-to-human) — the anti-proxy "level" problem is not a reason to discard the equilibrium-ish STRUCTURE human data provides. If from-scratch is retained at all, gate the switch on the exploiter probe or a pre-registered round-1 number (run both arms, adopt from-scratch iff BC fails to beat it by >=2pp non-overlapping), never on league macro alone.

=== SHOULD-FIX — BANK THESE INTO THE REWRITE (not blocking, but pre-register with the rest) ===
- Probe/slot budget: one outstanding request at a time; probe strongest-so-far (withdraw an earlier request if out-dominated); a total M3 slot ceiling agreed with Eddie; a rule for what a <=floor probe result does (count toward the falsification budget).
- CALIBRATION LEDGER: bank every probe as a local->ladder calibration point (2c1368bc03 = point #1: local 0.571 -> ladder-negative); after k probes refit §2/§2b against real data instead of n=1; where a slot can be spared, prefer probes that span the local-metric range so we learn the map's SHAPE, not just its intercept near 0.60.
- Confirm/reconfirm = AND of TWO independent n400 tests, NOT pooled-n800 (pooling lets a lucky confirm drag a mediocre reconfirm across, defeating the independence guard — d40d9dd567 precedent). Strike any "pooled n800 Wilson-lo>=0.55 suffices" wording.
- G4 windowed/cumulative (>2pp over ANY trailing 3-round window, not strictly consecutive) + a strength-referenced signal (halt if the current checkpoint loses Wilson-lo<0.50 to an incumbent from >=2 rounds ago). And run the exploiter probe every K rounds — halt if exploitability rises WHILE frozen-field macro rises (the feedback-loop failure G4 cannot see).
- Search variant: prefer ISMCTS over single-world PIMC; measure strategy-fusion bias (determinized-search value vs on-policy rollout on held-out high-entropy infosets); prefer on-policy MC rollout value targets where feasible (trade variance for bias deliberately). Note PTCG's high disambiguation factor may make determinized search empirically adequate — a reason to MEASURE exploitability, not assume it.
- Resource/failure envelope: throughput math WITH gate-pause downtime subtracted vs the §7 timeline; checkpoint/resume durability (episode-boundary persistence, deterministic RNG-state resume, round-interrupted-mid-gate = inadmissible); arena-1 single-box SPOF named + an outage runbook (the tailscale re-lock already happened once).
- Harvest-freshness invariant: each round summary records the harvest index age/hash; a staleness threshold FORCES a "C3 defense degraded: backbone stale" flag on that round's gate report; no probe request while the backbone is stale beyond threshold.
- Program compute/wall-clock ceiling beyond which M3 returns to Eddie/rpo for explicit continue-or-kill, independent of interim results; tie the held-band re-gate to it so held candidates cannot silently extend the program.
- League->gate trigger: pre-register the numeric frozen-field condition a checkpoint must meet before ANY §1 gate compute is spent (keep verdict LANGUAGE with the coordinator; bind the TRIGGER to a registered number).

=== WHAT PROCEEDS NOW (staged approval, mirroring the C1 staging) ===
- P3.0 (plumbing shakedown, torch training-venv, tokenizer/trainer skeleton, train/serve-identity test, BC-init diagnostic): PROCEEDS. Confirmed: no kill authority, no promotion, no probe slot. Keep going.
- P3.1 (league harness, pool loader, deck-refresh hook, per-round summary schema, G4 wiring): BUILD IT — but build the summary schema to carry what the blockers/must-fixes need (exploiter-probe result, per-decision s18 AND inference-time distributions, terminal-code distribution, harvest index age/hash, calibration-ledger fields) so you don't rework it.
- P3.2 FULL-CADENCE self-play spend: BLOCKED until blockers 1-4 + must-fixes 5-10 are pre-registered in the spec, and the 50k GO/NO-GO fires GO. This is the expensive, irreversible commitment; it waits for the exploitability spine and the terminal states to exist on paper first.
Nothing here says self-play is the wrong bet — it is the only bet left, and it is the right one. The rework makes it a bet we can actually READ: a loop that verifies its own improvement, a gate that measures exploitability and not just dominance-over-a-weak-champion, a crown that is defined, and a death line that lets the fifth lever die as honestly as the four before it. Rewrite to v2 against these, send it back, and I will review v2 against exactly this list. Good spec. Now make it one we can trust for three weeks unattended.